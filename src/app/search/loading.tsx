import Spinner from "@/components/Spinner";

export default function LoadingSearch(){
    return <div className="h-screen w-screen flex items-center justify-center mx-auto">
        <Spinner/>
    </div>
}